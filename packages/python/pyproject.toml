[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-scaffolds"
version = "0.2.0"
description = "Project scaffolding templates, tech stack configs, and initial file sets"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["scaffolds","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-scaffolds"
Repository = "https://github.com/mxn2020/minions-scaffolds"

[tool.hatch.build.targets.wheel]
packages = ["minions_scaffolds"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
